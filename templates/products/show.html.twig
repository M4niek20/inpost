{% extends 'base.html.twig' %}

{% block title %}{{ product.name }}{% endblock %}

{% block body %}
  <h1>Product info</h1>
  <p>id: {{ product.id }}</p>
  <p>Name: {{ product.name }}</p>
  <p>Barcode: {{ product.barcode }}</p>
  <h2>Owners</h2>
  {% for productOwner in product.getProducts %}
    {{productOwner.getOwner.getName }}
  {% endfor %}

  <hr/>
  <a href="/">Go back</a>
{% endblock %}